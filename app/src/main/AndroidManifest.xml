<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fixit">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FIXIT">
        <activity android:name=".Admin.fragments.ApproveTechFragmentAdmin"></activity>
        <activity android:name=".Admin.home.AdminHomeActivity" />
        <activity android:name=".technician.home.TechnicianHomeActivity" />
        <activity android:name=".customer.home.CustomerHomeActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.InitialActivity" />
        <activity android:name=".activities.CustomerUploadActivity" />
        <activity android:name=".activities.CustomerHistoryActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>